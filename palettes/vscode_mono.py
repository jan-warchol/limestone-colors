import common_shades_config

# Variant based on fg and bg lightness of default VScode dark theme
name = "VScode-based mono"
background = (11, 0, 0)
foreground = (78, 0, 0)
shades = common_shades_config.shades_lightness
